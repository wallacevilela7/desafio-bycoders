spring:
  application:
    name: 'desafio-bycoders'

  datasource:
    url: 'jdbc:postgresql://localhost:5432/bycodersdb'
    username: 'myuser'
    password: 'secret'
    driver-class-name: 'org.postgresql.Driver'

  jpa:
    database-platform: 'org.hibernate.dialect.PostgreSQLDialect'
    show-sql: true
    hibernate:
      ddl-auto: update
